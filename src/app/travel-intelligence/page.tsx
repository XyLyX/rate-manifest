import type {Metadata} from "next";import Link from "next/link";import {NavBar} from "@/components/NavBar";import {Footer} from "@/components/Footer";import {editorialArticles,editorialCategories} from "@/lib/editorial/articles";import styles from "./editorial.module.css";export const metadata:Metadata={title:"Travel Intelligence | Rate Manifest",description:"Original destination guides, hotel decision guides and travel planning insights across the world.",alternates:{canonical:"/travel-intelligence"}};export default function Page(){return <><NavBar/><main className={styles.main}><p className={styles.eyebrow}>RATE MANIFEST EDITORIAL</p><h1>Travel Intelligence</h1><p className={styles.lead}>Explore destinations and understand the decisions behind a better journey.</p>{editorialCategories.map(c=><section key={c.id}><h2>{c.label}</h2><div className={styles.grid}>{editorialArticles.filter(a=>a.category===c.id).map(a=><Link className={styles.card} href={`/travel-intelligence/${a.slug}`} key={a.slug}><div className={styles.image} style={{backgroundImage:`url("${a.image}")`}} role="img" aria-label={a.imageAlt}/><div className={styles.cardBody}><small>{a.place}</small><h3>{a.title}</h3><p>{a.intro}</p><strong>Read article →</strong></div></Link>)}</div></section>)}</main><Footer/></>}