import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {NavBar} from "@/components/NavBar";import {Footer} from "@/components/Footer";import {editorialArticles,getEditorialArticle} from "@/lib/editorial/articles";import styles from "../editorial.module.css";export function generateStaticParams(){return editorialArticles.map(a=>({slug:a.slug}));}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const a=getEditorialArticle(slug);return a?{title:`${a.title} | Rate Manifest`,description:a.intro,alternates:{canonical:`/travel-intelligence/${slug}`},openGraph:{title:a.title,description:a.intro,type:"article",url:`https://ratemanifest.com/travel-intelligence/${slug}`}}:{title:"Article not found"};}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=getEditorialArticle(slug);if(!a)notFound();return <><NavBar/><main className={styles.article}><Link href="/travel-intelligence" className={styles.back}>← All Travel Intelligence</Link><p className={styles.eyebrow}>{a.place}</p><h1>{a.title}</h1><p className={styles.lead}>{a.intro}</p><div className={styles.articleImage} style={{backgroundImage:`url("${a.image}")`}} role="img" aria-label={a.imageAlt}/><p className={styles.note}>Illustrative imagery. This guide offers general decision support; verify local conditions, opening hours and transport schedules before travelling.</p>{a.sections.map(s=><section key={s.heading}><h2>{s.heading}</h2><p>{s.body}</p></section>)}<div className={styles.bottom}><h2>Turn research into a decision</h2><p>Use Rate Manifest to shortlist and compare factual property details in supported destinations. Hotel rates and availability are not currently shown.</p><Link href="/hotels">Explore hotel discovery →</Link></div></main><Footer/></>}