"use client";
import {useState} from "react";
import Link from "next/link";
import {editorialArticles,editorialCategories,type EditorialCategory} from "@/lib/editorial/articles";
import styles from "./TravelIntelligence.module.css";
export function TravelIntelligence(){const [active,setActive]=useState<EditorialCategory>("destination");const items=editorialArticles.filter(a=>a.category===active);return <section className={styles.section} aria-labelledby="editorial-title"><div className={styles.inner}><div className={styles.heading}><div><p className={styles.eyebrow}>TRAVEL INTELLIGENCE</p><h2 id="editorial-title">Ideas, insights and smarter travel decisions</h2><p>Original travel intelligence for better journeys, wherever you go.</p></div><Link href="/travel-intelligence" className={styles.explore}>Explore all articles →</Link></div><div className={styles.tabs} role="tablist" aria-label="Travel Intelligence categories">{editorialCategories.map(c=><button key={c.id} type="button" role="tab" aria-selected={active===c.id} onClick={()=>setActive(c.id)} className={active===c.id?styles.selected:styles.tab}><strong>{c.label}</strong><small>{c.description}</small></button>)}</div><div className={styles.cards}>{items.map(a=><Link key={a.slug} href={`/travel-intelligence/${a.slug}`} className={styles.card}><div className={styles.copy}><span>{a.place}</span><h3>{a.title}</h3><p>{a.intro}</p><b>Read article →</b></div><div className={styles.photo} style={{backgroundImage:`url("${a.image}")`}} role="img" aria-label={a.imageAlt}/></Link>)}</div></div></section>}
